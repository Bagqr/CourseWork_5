<UserControl x:Class="BusParkManagementSystem.Views.UserControls.UsersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </UserControl.Resources>

    <Grid>
        <!-- Индикатор загрузки -->
        <Border Background="#80000000" 
                Visibility="{Binding IsLoading, Converter={StaticResource BoolToVis}}"
                Panel.ZIndex="100">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <ProgressBar IsIndeterminate="True" 
                             Width="200" 
                             Height="20" 
                             Margin="0,0,0,10"/>
                <TextBlock Text="Загрузка данных..." 
                           Foreground="White" 
                           FontSize="16"
                           HorizontalAlignment="Center"/>
            </StackPanel>
        </Border>

        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="16">
                <TextBlock Text="Управление пользователями" 
                           FontSize="24" 
                           FontWeight="Bold" 
                           Margin="0,0,0,16"
                           Foreground="#2C3E50"/>

                <!-- Панель инструментов -->
                <Border Style="{StaticResource CardStyle}">
                    <StackPanel>
                        <TextBlock Text="Действия с пользователями" 
                                   FontSize="16" 
                                   FontWeight="SemiBold"
                                   Margin="0,0,0,12"/>

                        <WrapPanel>
                            <Button Content="➕ Добавить" 
                                    Style="{StaticResource SuccessButton}"
                                    Width="120" 
                                    Height="40"
                                    Click="AddUser_Click"/>
                            <Button Content="✏️ Редактировать" 
                                    Style="{StaticResource PrimaryButton}"
                                    Width="140" 
                                    Height="40"
                                    Margin="12,0,0,0"
                                    Click="EditUser_Click"/>
                            <Button Content="🗑️ Удалить" 
                                    Style="{StaticResource DangerButton}"
                                    Width="100" 
                                    Height="40"
                                    Margin="12,0,0,0"
                                    Click="DeleteUser_Click"/>
                            <Button Content="🔑 Права доступа" 
                                    Style="{StaticResource PrimaryButton}"
                                    Width="140" 
                                    Height="40"
                                    Margin="12,0,0,0"
                                    Click="ManagePermissions_Click"/>
                            <Button Content="🔄 Обновить" 
                                    Style="{StaticResource PrimaryButton}"
                                    Width="120" 
                                    Height="40"
                                    Margin="12,0,0,0"
                                    Click="Refresh_Click"/>
                        </WrapPanel>
                    </StackPanel>
                </Border>

                <!-- Список пользователей -->
                <Border Style="{StaticResource CardStyle}" Margin="0,16,0,0">
                    <StackPanel>
                        <TextBlock Text="Список пользователей" 
                                   FontSize="16" 
                                   FontWeight="SemiBold"
                                   Margin="0,0,0,12"/>

                        <DataGrid x:Name="UsersGrid"
                                  AutoGenerateColumns="False" 
                                  CanUserAddRows="False"
                                  SelectionMode="Single"
                                  IsReadOnly="True"
                                  Height="400"
                                  Margin="0,0,0,10">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                                <DataGridTextColumn Header="Логин" Binding="{Binding Username}" Width="150"/>
                                <DataGridTextColumn Header="Роль" Binding="{Binding Role}" Width="150"/>
                                <DataGridCheckBoxColumn Header="Активен" Binding="{Binding IsActive}" Width="70"/>
                                <DataGridTextColumn Header="Дата создания" Binding="{Binding CreatedAt, StringFormat=dd.MM.yyyy}" Width="100"/>
                                <DataGridTextColumn Header="Последний вход" Binding="{Binding LastLogin, StringFormat=dd.MM.yyyy HH:mm}" Width="120"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>