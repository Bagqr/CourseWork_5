<UserControl x:Class="BusParkManagementSystem.Views.UserControls.AccessDeniedView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="400" d:DesignWidth="600">

    <Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock Text="🚫" 
                       FontSize="72" 
                       HorizontalAlignment="Center"
                       Margin="0,0,0,20"/>

            <TextBlock Text="ДОСТУП ЗАПРЕЩЕН" 
                       FontSize="28" 
                       FontWeight="Bold"
                       Foreground="#E74C3C"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,10"/>

            <TextBlock Text="У вас недостаточно прав для доступа к этому разделу" 
                       FontSize="16"
                       HorizontalAlignment="Center"
                       TextAlignment="Center"
                       Margin="0,0,0,30"/>

            <TextBlock x:Name="RoleInfoText"
                       FontSize="14"
                       HorizontalAlignment="Center"
                       TextAlignment="Center"
                       Margin="0,0,0,20"/>

            <Button Content="Вернуться на главную" 
                    Style="{StaticResource PrimaryButton}"
                    Width="200"
                    Height="40"
                    Click="BackButton_Click"/>
        </StackPanel>
    </Grid>
</UserControl>