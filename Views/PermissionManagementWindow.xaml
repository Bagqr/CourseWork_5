<Window x:Class="BusParkManagementSystem.PermissionManagementWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Управление правами доступа" 
        Height="600" Width="800"
        WindowStartupLocation="CenterOwner"
        Background="#ECF0F1">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Панель инструментов -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <ComboBox x:Name="RoleComboBox" Width="150" Height="30" Margin="0,0,10,0"/>
            <Button Content="Загрузить шаблон прав" Width="150" Height="30" Margin="0,0,10,0"/>
            <Button Content="Сохранить шаблон" Width="150" Height="30"/>
        </StackPanel>

        <!-- Таблица прав для ролей -->
        <DataGrid x:Name="RolePermissionsGrid" Grid.Row="1" 
                  AutoGenerateColumns="False" 
                  CanUserAddRows="False"
                  IsReadOnly="False" 
                  Margin="0,0,0,10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Модуль" Width="200" IsReadOnly="True"/>
                <DataGridCheckBoxColumn Header="Чтение (R)"/>
                <DataGridCheckBoxColumn Header="Запись (W)"/>
                <DataGridCheckBoxColumn Header="Редактирование (E)"/>
                <DataGridCheckBoxColumn Header="Удаление (D)"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Кнопки -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Применить ко всем" Width="120" Height="30" Margin="0,0,10,0"/>
            <Button Content="Сохранить" Width="100" Height="30" Margin="0,0,10,0"/>
            <Button Content="Закрыть" Width="100" Height="30"/>
        </StackPanel>
    </Grid>
</Window>