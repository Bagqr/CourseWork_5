<Window x:Class="BusParkManagementSystem.UserManagementWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Управление пользователями" 
        Height="600" Width="800"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#ECF0F1">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Панель инструментов -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <Button Content="Добавить" Width="100" Height="30" Margin="0,0,10,0" Click="AddUser_Click"/>
            <Button Content="Изменить" Width="100" Height="30" Margin="0,0,10,0" Click="EditUser_Click"/>
            <Button Content="Удалить" Width="100" Height="30" Margin="0,0,10,0" Click="DeleteUser_Click"/>
            <Button Content="Сброс пароля" Width="100" Height="30" Margin="0,0,10,0" Click="ResetPassword_Click"/>
            <Button Content="Права доступа" Width="120" Height="30" Click="ManagePermissions_Click"/>
        </StackPanel>

        <!-- Список пользователей -->
        <DataGrid x:Name="UsersGrid" Grid.Row="1" 
                  AutoGenerateColumns="False" 
                  CanUserAddRows="False"
                  SelectionMode="Single"
                  IsReadOnly="True"
                  Margin="0,0,0,10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                <DataGridTextColumn Header="Логин" Binding="{Binding Username}" Width="150"/>
                <DataGridTextColumn Header="Роль" Binding="{Binding Role}" Width="150"/>
                <DataGridCheckBoxColumn Header="Активен" Binding="{Binding IsActive}" Width="70"/>
                <DataGridTextColumn Header="Дата создания" Binding="{Binding CreatedAt, StringFormat=dd.MM.yyyy}" Width="100"/>
                <DataGridTextColumn Header="Последний вход" Binding="{Binding LastLogin, StringFormat=dd.MM.yyyy HH:mm}" Width="120"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Кнопки управления -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Обновить" Width="100" Height="30" Margin="0,0,10,0" Click="Refresh_Click"/>
            <Button Content="Закрыть" Width="100" Height="30" Click="Close_Click"/>
        </StackPanel>
    </Grid>
</Window>