<Window x:Class="BusParkManagementSystem.Views.Dialogs.TripCompletionDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:converters="clr-namespace:BusParkManagementSystem.Converters"
        Title="Завершение рейса" 
        Height="400" 
        Width="500"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#F5F5F5">

    <Window.Resources>
        <converters:DecimalConverter x:Key="DecimalConverter"/>
        <converters:IntConverter x:Key="IntConverter"/>

        <BooleanToVisibilityConverter x:Key="BoolToVis"/>

        <Style TargetType="TextBlock">
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Height" Value="30"/>
        </Style>
    </Window.Resources>

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <Border Grid.Row="0" Background="#2C3E50" CornerRadius="3" Margin="0,0,0,15">
            <TextBlock Text="Завершение рейса" 
                       FontSize="18" 
                       FontWeight="Bold"
                       Foreground="White"
                       HorizontalAlignment="Center"
                       Padding="10"/>
        </Border>

        <!-- Форма ввода -->
        <Border Grid.Row="1" 
                BorderBrush="#BDBDBD" 
                BorderThickness="1" 
                CornerRadius="5"
                Background="White"
                Padding="15">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" MinWidth="180"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Информация о рейсе -->
                <TextBlock Grid.Row="0" Grid.Column="0" Text="Информация о рейсе:" 
                           Margin="0,0,10,10"/>
                <TextBlock Grid.Row="0" Grid.Column="1" 
                           Text="{Binding TripInfo}"
                           TextWrapping="Wrap"
                           FontStyle="Italic"
                           Margin="0,0,0,10"/>

                <!-- Плановая выручка -->
                <TextBlock Grid.Row="1" Grid.Column="0" Text="Плановая выручка:" 
                           Margin="0,0,10,10"/>
                <TextBlock Grid.Row="1" Grid.Column="1" 
                           Text="{Binding PlannedRevenueDisplay}"
                           FontWeight="Bold"
                           Margin="0,0,0,10"/>

                <!-- Фактическая выручка -->
                <TextBlock Grid.Row="2" Grid.Column="0" Text="Фактическая выручка*:" 
                           Margin="0,0,10,10"/>
                <TextBox Grid.Row="2" Grid.Column="1" 
                         Text="{Binding ActualRevenue, UpdateSourceTrigger=PropertyChanged, 
                                Converter={StaticResource DecimalConverter}}"
                         Margin="0,0,0,10"
                         ToolTip="Введите фактическую выручку (например: 14500.75)"/>

                <!-- Продано билетов -->
                <TextBlock Grid.Row="3" Grid.Column="0" Text="Продано билетов*:" 
                           Margin="0,0,10,0"/>
                <TextBox Grid.Row="3" Grid.Column="1" 
                         Text="{Binding TicketsSold, UpdateSourceTrigger=PropertyChanged, 
                                Converter={StaticResource IntConverter}}"
                         ToolTip="Введите количество проданных билетов"/>
            </Grid>
        </Border>

        <!-- Панель ошибок -->
        <Border Grid.Row="2" 
                Background="#FFF2DEDE" 
                BorderBrush="#E6B3B3" 
                BorderThickness="1" 
                CornerRadius="5"
                Padding="10,8"
                Margin="0,10,0,10"
                Visibility="{Binding HasErrors, Converter={StaticResource BoolToVis}}">
            <StackPanel>
                <TextBlock Text="⚠️ Ошибки валидации:" 
                           FontWeight="Bold" 
                           Foreground="#A94442"
                           Margin="0,0,0,5"/>
                <ItemsControl ItemsSource="{Binding Errors}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding}" 
                                       TextWrapping="Wrap"
                                       Foreground="#A94442"
                                       Margin="5,2,0,2"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </Border>

        <!-- Кнопки -->
        <Border Grid.Row="3" 
                BorderBrush="#E0E0E0" 
                BorderThickness="0,1,0,0"
                Background="#F8F8F8"
                Padding="10">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Отмена" 
                        Width="120" 
                        Height="35"
                        Margin="0,0,10,0"
                        Command="{Binding CancelCommand}"
                        Background="#F5F5F5"
                        BorderBrush="#CCCCCC"
                        Foreground="#333333"/>

                <Button Content="Завершить" 
                        Width="120" 
                        Height="35"
                        Command="{Binding CompleteCommand}"
                        IsEnabled="{Binding CanComplete}"
                        Background="#4CAF50"
                        Foreground="White"
                        FontWeight="Bold"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>