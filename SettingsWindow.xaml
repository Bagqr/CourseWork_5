<Window x:Class="BusParkManagementSystem.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Разное - Настройки" 
        Height="400" 
        Width="500"
        WindowStartupLocation="CenterOwner"
        Background="#ECF0F1">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <Border Grid.Row="0" 
                Background="#2C3E50"
                Padding="16"
                CornerRadius="4">
            <StackPanel>
                <TextBlock Text="⚙️ РАЗНОЕ" 
                           FontSize="20" 
                           FontWeight="Bold" 
                           Foreground="White"
                           HorizontalAlignment="Center"/>
            </StackPanel>
        </Border>

        <!-- Основное содержимое -->
        <Border Grid.Row="1" 
                Margin="0,16,0,0"
                Background="White"
                CornerRadius="4"
                BorderBrush="#DDDDDD"
                BorderThickness="1">
            <StackPanel Margin="20">
                <TextBlock Text="ДОПОЛНИТЕЛЬНЫЕ НАСТРОЙКИ" 
                           FontSize="18" 
                           FontWeight="Bold" 
                           Foreground="#2C3E50"
                           Margin="0,0,0,20"
                           HorizontalAlignment="Center"/>

                <!-- Настройка -->
                <Border BorderBrush="#3498DB" 
                        BorderThickness="2" 
                        CornerRadius="4"
                        Padding="15"
                        Margin="0,0,0,15"
                        Background="#E8F4F8">
                    <StackPanel>
                        <TextBlock Text="🔧 НАСТРОЙКА" 
                                   FontSize="16" 
                                   FontWeight="Bold"
                                   Foreground="#2980B9"
                                   Margin="0,0,0,10"/>
                        <TextBlock TextWrapping="Wrap" FontSize="14" Margin="0,0,0,10">
                            Настройки приложения, включая:<LineBreak/>
                            • Смену языка интерфейса<LineBreak/>
                            • Увеличение или уменьшение шрифта<LineBreak/>
                            • Другие параметры отображения
                        </TextBlock>
                        <Button x:Name="BtnLanguage" 
                                Content="Смена языка" 
                                Style="{StaticResource PrimaryButton}" 
                                HorizontalAlignment="Left"
                                Margin="0,5,0,0"
                                Click="BtnLanguage_Click"/>
                        <Button x:Name="BtnFontSize" 
                                Content="Изменить размер шрифта" 
                                Style="{StaticResource PrimaryButton}" 
                                HorizontalAlignment="Left"
                                Margin="0,5,0,0"
                                Click="BtnFontSize_Click"/>
                    </StackPanel>
                </Border>

                <!-- Сменить пароль -->
                <Border BorderBrush="#E74C3C" 
                        BorderThickness="2" 
                        CornerRadius="4"
                        Padding="15"
                        Margin="0,0,0,15"
                        Background="#FDEDEC">
                    <StackPanel>
                        <TextBlock Text="🔐 СМЕНИТЬ ПАРОЛЬ" 
                                   FontSize="16" 
                                   FontWeight="Bold"
                                   Foreground="#C0392B"
                                   Margin="0,0,0,10"/>
                        <TextBlock TextWrapping="Wrap" FontSize="14" Margin="0,0,0,10">
                            Изменить текущий пароль учетной записи.<LineBreak/>
                            Для смены пароля потребуется ввести<LineBreak/>
                            текущий пароль и новый дважды.
                        </TextBlock>
                        <Button x:Name="BtnChangePassword" 
                                Content="Сменить пароль" 
                                Style="{StaticResource PrimaryButton}" 
                                HorizontalAlignment="Left"
                                Margin="0,5,0,0"
                                Click="BtnChangePassword_Click"/>
                    </StackPanel>
                </Border>

                <!-- Дополнительные настройки -->
                <Border BorderBrush="#2ECC71" 
                        BorderThickness="2" 
                        CornerRadius="4"
                        Padding="15"
                        Background="#E8F8F5">
                    <StackPanel>
                        <TextBlock Text="📋 ДРУГИЕ НАСТРОЙКИ" 
                                   FontSize="16" 
                                   FontWeight="Bold"
                                   Foreground="#27AE60"
                                   Margin="0,0,0,10"/>
                        <TextBlock TextWrapping="Wrap" FontSize="14" Margin="0,0,0,10">
                            Дополнительные настройки приложения:<LineBreak/>
                            • Резервное копирование данных<LineBreak/>
                            • Экспорт отчетов<LineBreak/>
                            • Проверка обновлений
                        </TextBlock>
                        <Button x:Name="BtnBackup" 
                                Content="Резервное копирование" 
                                Style="{StaticResource PrimaryButton}" 
                                HorizontalAlignment="Left"
                                Margin="0,5,0,0"
                                Click="BtnBackup_Click"/>
                    </StackPanel>
                </Border>
            </StackPanel>
        </Border>

        <!-- Кнопка закрытия -->
        <StackPanel Grid.Row="2" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right"
                    Margin="0,16,0,0">
            <Button Content="Закрыть" 
                    Style="{StaticResource DangerButton}"
                    Width="100"
                    Height="36"
                    Click="BtnClose_Click"/>
        </StackPanel>
    </Grid>
</Window>